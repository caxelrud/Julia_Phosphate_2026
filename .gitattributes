# Normalise line endings so that the notebooks (whose format is line based) and
# the Julia sources behave the same on every platform.
* text=auto eol=lf
*.jl text eol=lf
*.toml text eol=lf
*.md text eol=lf
*.yml text eol=lf
*.pdf binary
*.png binary
